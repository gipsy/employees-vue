export interface User {
  //id: string;
  name: string;
  email: string;
  password: string;
}

export interface Employee {
  //id: string;
  address: string;
  age: string;
  firstName: string;
  lastName: string;
  userId: string;
}
